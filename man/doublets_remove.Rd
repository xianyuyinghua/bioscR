% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doublets_remove.R
\name{doublets_remove}
\alias{doublets_remove}
\title{Detect and optionally remove doublets from a Seurat object}
\usage{
doublets_remove(
  seu,
  assay = "RNA",
  sample_col = "sample",
  remove_doublets = TRUE,
  seed = 123,
  expected_doublet_rate = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{seu}{A Seurat object containing a non-empty raw counts layer in the
selected assay.}

\item{assay}{A character scalar naming the assay from which raw counts are
obtained. Defaults to \code{"RNA"}.}

\item{sample_col}{A character scalar naming a column in \code{seu} metadata that
identifies independent samples or capture batches. Use \code{NULL} when all
cells belong to a single sample. Defaults to \code{"sample"}.}

\item{remove_doublets}{A logical scalar. If \code{TRUE} (the default), return
only cells classified as singlets. If \code{FALSE}, retain all cells and only
add the doublet annotations.}

\item{seed}{A value passed to \code{set.seed()} before running doublet detection.
Defaults to \code{123}.}

\item{expected_doublet_rate}{An optional expected doublet rate supplied as
the \code{dbr} argument to \code{scDblFinder::scDblFinder()}. If \code{NULL},
\code{scDblFinder} estimates or selects its default rate.}

\item{verbose}{A logical scalar controlling progress messages from this
function and from \code{scDblFinder}. Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to \code{scDblFinder::scDblFinder()}.}
}
\value{
A Seurat object containing the added \code{scDblFinder.*}, \code{is_doublet},
and \code{is_singlet} metadata columns. When \code{remove_doublets = TRUE}, the
returned object contains only cells classified as singlets; otherwise it
contains all input cells.
}
\description{
Runs \code{scDblFinder::scDblFinder()} on the raw count matrix of a Seurat
object, adds the doublet predictions to the cell-level metadata, and
optionally retains only cells classified as singlets. When the object
contains multiple independently captured samples, \code{sample_col} should
identify those samples so that doublet detection is performed accordingly.
}
\details{
Raw counts are read from the \code{counts} layer (Seurat v5), with a fallback to
the \code{counts} slot used by earlier Seurat versions. If a Seurat v5 assay has
multiple \code{counts.*} layers, they are first joined into a single \code{counts}
layer with \code{SeuratObject::JoinLayers()}. The count matrix is then checked
and reordered to match the cells in the Seurat object. The function converts
these counts to a \code{SingleCellExperiment}, runs \code{scDblFinder}, and copies
all output columns whose names begin with \code{scDblFinder.} back to the Seurat
metadata. It also creates the logical metadata columns \code{is_doublet} and
\code{is_singlet}.

If \code{remove_doublets = TRUE}, cells classified as doublets and cells without
a singlet classification are removed. Existing assays, reductions, graphs,
and metadata are otherwise retained through Seurat subsetting.
}
\examples{
\dontrun{
# Detect doublets separately for each sample and remove them.
seu <- doublets_remove(seu, sample_col = "sample")

# Annotate a single-library object without removing any cells.
seu <- doublets_remove(
  seu,
  sample_col = NULL,
  remove_doublets = FALSE
)
}
}
\seealso{
\code{scDblFinder::scDblFinder()}
}
